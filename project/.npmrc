; NPM config for using GitHub Packages for @aadilkhandev scope
; Do NOT commit tokens to the repo. Use environment variables or CI secrets.
@aadilkhandev:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

; If you want to prefer the public registry for other packages, you can set it explicitly
registry=https://registry.npmjs.org/
